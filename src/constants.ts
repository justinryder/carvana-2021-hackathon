
export const PACKET_WIDTH = 170;
export const PACKET_HEIGHT = 200;

const bucketScalar = 1.3;
export const BUCKET_WIDTH = PACKET_WIDTH * bucketScalar;
export const BUCKET_HEIGHT = PACKET_HEIGHT * bucketScalar;


